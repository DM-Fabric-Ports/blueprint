package com.teamabnormals.blueprint.core.util.registry.core;

import com.dm.earth.deferred_registries.DeferredRegistries;
import net.minecraft.core.Registry;
import net.minecraft.resources.ResourceKey;
import net.minecraft.world.level.biome.Biome;

public class DeferredBiomeRegistries extends DeferredRegistries<Biome> {

	public DeferredBiomeRegistries(String modId) {
	}
}
